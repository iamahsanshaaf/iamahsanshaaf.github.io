(()=>{const root=document.documentElement,header=document.querySelector('[data-header]'),progress=document.querySelector('[data-scroll-progress]'),menuBtn=document.querySelector('[data-menu-button]'),mobileMenu=document.querySelector('[data-mobile-menu]'),filters=document.querySelectorAll('[data-filter]'),cards=document.querySelectorAll('[data-platform]'),year=document.querySelector('[data-year]'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(year)year.textContent=new Date().getFullYear();const onScroll=()=>{header?.classList.toggle('scrolled',scrollY>14);if(progress){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?Math.min(100,(scrollY/max)*100):0}%`;}};onScroll();addEventListener('scroll',onScroll,{passive:true});const closeMenu=()=>{menuBtn?.setAttribute('aria-expanded','false');mobileMenu?.classList.remove('open');document.body.classList.remove('menu-open')};menuBtn?.addEventListener('click',()=>{const open=menuBtn.getAttribute('aria-expanded')==='true';menuBtn.setAttribute('aria-expanded',String(!open));mobileMenu?.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));addEventListener('resize',()=>{if(innerWidth>900)closeMenu()});filters.forEach(btn=>btn.addEventListener('click',()=>{const f=btn.dataset.filter;filters.forEach(b=>b.classList.toggle('active',b===btn));cards.forEach(card=>{const show=f==='all'||card.dataset.platform===f;card.classList.toggle('hidden',!show)})}));document.querySelectorAll('.spotlight-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--mx',`${e.clientX-r.left}px`);card.style.setProperty('--my',`${e.clientY-r.top}px`)})});if(!reduce){const tilt=document.querySelector('[data-tilt-card]');tilt?.addEventListener('pointermove',e=>{const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;tilt.style.transform=`perspective(900px) rotateY(${x*3.5}deg) rotateX(${-y*3.5}deg)`});tilt?.addEventListener('pointerleave',()=>tilt.style.transform='');}const reveals=document.querySelectorAll('.reveal');if(reduce||!('IntersectionObserver'in window)){reveals.forEach(el=>el.classList.add('visible'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('visible');observer.unobserve(entry.target)}),{threshold:.08,rootMargin:'0px 0px -32px'});reveals.forEach((el,i)=>{el.dataset.stagger=String((i%3)+1);observer.observe(el)})}const navLinks=[...document.querySelectorAll('.desktop-nav a')];const sections=navLinks.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);if('IntersectionObserver'in window){const navObserver=new IntersectionObserver(entries=>{entries.forEach(e=>{if(!e.isIntersecting)return;navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${e.target.id}`))})},{rootMargin:'-35% 0px -55% 0px',threshold:0});sections.forEach(s=>navObserver.observe(s))}
// Load current Apple artwork from Apple's public Lookup API. Fallback SVGs remain if the network/API is unavailable.
const appleImgs=[...document.querySelectorAll('img.apple-artwork[data-apple-id]')];const setArtwork=(img,data)=>{const item=data?.results?.[0];const url=item?.artworkUrl512||item?.artworkUrl100;if(url){img.src=url;img.dataset.remoteArtwork='loaded'}};const jsonpLookup=(img,id)=>{const cb=`__appleArtwork_${id}_${Math.random().toString(36).slice(2)}`;const script=document.createElement('script');let done=false;const cleanup=()=>{if(done)return;done=true;delete window[cb];script.remove()};window[cb]=data=>{setArtwork(img,data);cleanup()};script.src=`https://itunes.apple.com/lookup?id=${encodeURIComponent(id)}&country=us&callback=${cb}`;script.onerror=cleanup;document.head.appendChild(script);setTimeout(cleanup,8000)};appleImgs.forEach(async img=>{const id=img.dataset.appleId;try{const res=await fetch(`https://itunes.apple.com/lookup?id=${encodeURIComponent(id)}&country=us`,{mode:'cors'});if(!res.ok)throw new Error('lookup');setArtwork(img,await res.json())}catch(_){jsonpLookup(img,id)}})})();